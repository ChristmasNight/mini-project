<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="boardMapper">
   
   <!-- 게시판 글쓰기 -->
   <insert id="insert">
	   insert into mp_board(bno, title, content, writer)
	   	values(mp_board_seq.nextval
	   			, #{title}
	   			, #{content}
	   			, #{writer})
   </insert>
   
    <!-- 게시판 목록 -->   
   <select id="list" resultType="com.nxon.vo.BoardVO">
	   	select * from mp_board
	   		order by bno desc   
   </select>
   
   <!-- 게시판 상세보기 -->   
   <select id="read" parameterType="int" resultType="com.nxon.vo.BoardVO">
		SELECT	BNO
			  , TITLE
			  , CONTENT
			  , WRITER
			  , REGDATE
		 FROM MP_BOARD
		 WHERE BNO = #{bno}
	</select>
	
	<!-- 게시판 수정 -->   	
	<update id="update" parameterType="com.nxon.vo.BoardVO">
		UPDATE MP_BOARD
		   SET TITLE    =   #{title},
		   	   CONTENT  =   #{content}
		 WHERE BNO = #{bno} 
	</update>
	
	<!-- 게시판 삭제 -->   		
	<delete id="delete" parameterType="int">
		DELETE 
		  FROM MP_BOARD
		 WHERE BNO = #{bno}
	</delete>
	
   
</mapper>
